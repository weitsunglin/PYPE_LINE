import os

    # Debug_Opt、Release_Opt 測試
    # directory = "F:\_project"
    # print(f"Listing files in directory: {directory}")
    # for root, dirs, files in os.walk(directory):
    #     for file in files:
    #         print(os.path.join(root, file))

def run():
    print("Debug_Opt、Release_Opt 測試")